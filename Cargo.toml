[package]
name = "omer"
version = "0.2.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }
serde-untagged = { version = "0.1", optional = true }

[features]
default = ["serde"]
serde = ["dep:serde", "dep:serde-untagged"]
